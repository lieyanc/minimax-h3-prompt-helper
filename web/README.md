# Frontend

React + Tailwind + shadcn/ui source for the H3 prompt helper.

`npm run build` writes into `../webui/dist`, which the Go binary embeds.
Use `npm run dev` alongside a running `go run .` for local development.
